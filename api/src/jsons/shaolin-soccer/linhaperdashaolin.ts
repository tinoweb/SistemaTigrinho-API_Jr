export default {
   async linhaperda() {
      const numeroAleatorio: number = Math.floor(Math.random() * 68) + 1
      console.log("LINHA DE PERDA " + numeroAleatorio)

      const jsons: any = {
       
      }
      return jsons[numeroAleatorio]
   },
}
